name: android-build
on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Flutter 3.24.0 (stable)
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: "3.24.0"

      - name: Env check
        run: |
          flutter --version
          flutter doctor -v

      - name: Clean & deps
        run: |
          rm -rf build android/app/build
          flutter clean
          flutter pub get

      # Camino 1: flutter produce APK y AAB
      - name: Build with Flutter
        run: |
          set -e
          # No fallar el job si uno de los dos no se genera
          flutter build apk --release || true
          flutter build appbundle --release || true

      # Camino 2 (fallback): Gradle directo (por si Flutter no recoge el artefacto)
      - name: Build with Gradle (fallback)
        working-directory: android
        run: |
          set -e
          ./gradlew --version
          ./gradlew :app:assembleRelease || true
          ./gradlew :app:bundleRelease || true

      - name: List outputs (debug)
        run: |
          echo "::group::Outputs under build/"
          find build -type f \( -name "*.apk" -o -name "*.aab" \) -print || true
          echo "::endgroup::"
          echo "::group::Outputs under android/app/build/"
          find android/app/build -type f \( -name "*.apk" -o -name "*.aab" \) -print || true
          echo "::endgroup::"

      - name: Upload APKs (catch-all)
        uses: actions/upload-artifact@v4
        with:
          name: apks
          if-no-files-found: warn
          path: |
            build/app/outputs/flutter-apk/*release*.apk
            build/**/outputs/**/*.apk
            build/**/*.apk
            android/app/build/outputs/**/*.apk

      - name: Upload AABs (catch-all)
        uses: actions/upload-artifact@v4
        with:
          name: aabs
          if-no-files-found: warn
          path: |
            build/app/outputs/bundle/release/*.aab
            build/**/outputs/**/*.aab
            build/**/*.aab
            android/app/build/outputs/**/*.aab